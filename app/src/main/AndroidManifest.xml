<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 앱이 웹 대신 하는 일은 녹음 하나뿐이다. 필요한 권한도 그 주변으로만 둔다. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- 녹음 중 알림. 이 알림이 떠 있어야 화면을 꺼도 시스템이 녹음을 재우지 않는다 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 화면을 끈 채 몇 시간을 녹음해도 CPU 가 잠들지 않게 한다 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 삼성 절전이 백그라운드 앱을 재우는 것을 사용자가 직접 풀 수 있게 한다 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 웹 화면을 열고, 녹음을 서버로 올린다 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 홈 화면에서 앱 아이콘을 길게 누르면 나오는 '서버 주소' 바로가기 -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <service
            android:name=".RecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />
    </application>

</manifest>
